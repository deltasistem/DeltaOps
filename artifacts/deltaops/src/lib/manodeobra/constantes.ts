/**
 * DGP-020.3 · Constantes del módulo Mano de Obra (frontend).
 */

/** Namespace de comandos del módulo (para /sync si algún día se encola). */
export const MODULO = "modulo.manodeobra";

/** Base HTTP del módulo (las rutas cuelgan de aquí). */
export const API_BASE = "/api/deltaops/manodeobra";

/** Catálogo canónico de categorías de mano de obra. */
export const CATALOGO_CATEGORIAS = "categorias-mdo";
