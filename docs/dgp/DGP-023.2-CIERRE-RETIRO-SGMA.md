# DGP-023.2 — CIERRE · RETIRO DEFINITIVO DE SGMA

> **Aprobación explícita de Dirección (DGP-023.1 PASS).** SGMA nunca llegó a producción; fue reemplazado por DeltaOps (producto único). Este documento cierra la implementación del retiro. Fecha: 2026-08-13. **NO commit** (lo orquesta Dirección tras revisión). **NO se inició DGP-023.3.**

Base de referencia: tag **`pre-retiro-sgma`** (HEAD `254405448433f5c042d955b57942d9fe7e5004ab`, rama `main`).

---

## 1. Resumen ejecutivo — VERDICTO: **DGP-023.2 PASS**

SGMA se retiró por completo (frontend, API legacy, tablas `public.*`, seed, schemas Drizzle, contratos OpenAPI exclusivos y clientes generados) sin afectar a DeltaOps. Todas las validaciones obligatorias verdes salvo una suite PG **preexistentemente flaky** (`module-ordenes`) demostrada sin relación con este retiro (código byte-idéntico al baseline, cero dependencias SGMA).

## 2. Qué se ELIMINÓ

| Elemento | Detalle |
|---|---|
| **Frontend** | Directorio completo `artifacts/sgma/` (11 páginas, 58 UI, 3 componentes, 2 hooks, 2 lib, config, `public/`, `.replit-artifact/artifact.toml`). Su workflow `artifacts/sgma: web` fue retirado automáticamente por el sistema de artefactos. |
| **API legacy** | 10 routers + agregador: `routes/{health,dashboard,assets,work-orders,maintenance-plans,spare-parts,locations,work-centers,technicians,suppliers,index}.ts`. Montaje en `app.ts` (`import router` + `app.use("/api", router)`) retirado. |
| **Tablas DB** | 9 tablas `public.*` (`assets, locations, maintenance_plans, spare_parts, stock_movements, suppliers, technicians, work_centers, work_orders`) + sus secuencias `*_id_seq` (owned, caen con las tablas). Migración `0048_retiro_sgma_public.sql`. |
| **Schemas Drizzle** | 9 archivos `lib/db/src/schema/{...}.ts` + poda del barrel `lib/db/src/schema/index.ts` (líneas SGMA). |
| **Seed** | `scripts/src/seed-sgma.ts` + entrada `seed-sgma` en `scripts/package.json`. |
| **OpenAPI** | Paths `/healthz` + `/dashboard/*` + CRUD de las 9 entidades (28 paths); schemas SGMA (`Asset*`, `WorkOrder*`, `MaintenancePlan*`, `SparePart*`, `StockMovement*`, `Location*`, `WorkCenter*`, `Technician*`, `Supplier*`, `Dashboard*`, `StatusCount`, `MonthlyCost`, `AssetCost`, `ActivityItem`, `HealthStatus`); tags SGMA. |
| **Clientes generados** | `api-client-react` y `api-zod` regenerados sin símbolos SGMA (37 archivos zod SGMA eliminados). |

## 3. Qué se CONSERVÓ (verificado)

- **DeltaOps completo**: `artifacts/deltaops/src` (byte-idéntico al baseline), `artifacts/api-server/src/routes/deltaops/**`, sus 12 módulos, esquema `deltaops.*` (174 tablas), RLS, identidad canónica, Analytics, Utilización, Mano de Obra, Costos.
- **Contratos compartidos**: `lib/api-spec/openapi.yaml` conservado (editado, no borrado) con `/deltaops/*` + **schema `Error`** (compartido por `/deltaops/auth/login|me`); paquetes `api-client-react`/`api-zod` conservados (regenerados).
- **mockup-sandbox**: intacto (`git diff` vs tag = vacío).
- **Documentación histórica SGMA**: `MODULOS_EXISTENTES.md`, `ARQUITECTURA_ACTUAL.md`, `REUTILIZACION.md` conservadas con banner "DOCUMENTO HISTÓRICO — SGMA retirado". `docs/dgp/*` sin tocar.

## 4. Backup (FASE 1)

- Archivo: `backups/sgma-public-pre-drop.sql` (**20 898 bytes**, 700 líneas). `backups/` añadido a `.gitignore` (git limpio).
- Contenido verificado: **9 `CREATE TABLE` + 9 `CREATE SEQUENCE` + todos los bloques `COPY`** con datos (p.ej. `work_orders` = 10 filas); termina con marcador "dump complete" (no truncado). Restaurable con `psql -f` / `pg_restore`.
- `pg_dump` ejecutado **antes** de cualquier DROP.

## 5. Health gate (FASE 2)

- `artifacts/api-server/.replit-artifact/artifact.toml` → `[services.production.health.startup] path` migrado de `/api/healthz` (legacy SGMA) a **`/api/deltaops/platform/health`** (ruta DeltaOps existente; no se creó una segunda ruta).
- Editado vía flujo `verifyAndReplaceArtifactToml` (`success:true`). Endpoint verificado responde **200** `{"status":"ok",...}`.

## 6. OpenAPI + clientes (FASE 5)

- Limpieza quirúrgica: tras la edición, `openapi.yaml` contiene 7 paths (`/deltaops/*`) y 8 schemas (`DeltaopsHealth/Check/Readiness/Info/Metrics/Credentials/SessionUser` + `Error`). Todos los `$ref` resuelven.
- Codegen (`pnpm --filter @workspace/api-spec run codegen`) OK; `typecheck:libs` verde. Verificado: **0 hooks/zod SGMA** en los generados; **`useDeltaops*` + `Error` presentes**.
- Drift OpenAPI DeltaOps: `openapi.test.ts` (valida `identity.openapi.json`, generador propio) **PASS** dentro de la suite api-server. No hay drift en `openapi.yaml` (regeneración determinista, git limpio).

## 7. Migración DB (FASE 7)

- Migración `lib/db/migrations/deltaops/0048_retiro_sgma_public.sql` — `DROP TABLE IF EXISTS` de SOLO las 9 tablas. **No** se usó `DROP SCHEMA CASCADE` ni comandos globales.
- Verificación de catálogo **pre-DROP** (re-confirmada): 0 FKs, 0 vistas, 0 triggers, 0 funciones en `public`.
- Aplicada con `psql --set ON_ERROR_STOP=1`. Verificación **post-DROP**: `public` = **0 tablas / 0 secuencias**; `deltaops` = **174 tablas** (intacto); `deltaops.users`=120, `deltaops.ten_tenants`=2 tras reseed.

## 8. Configuración y docs (FASES 8–9)

- Sin config SGMA remanente: `.replit`, `docker-compose.yml`, `package.json`, `.github/workflows/ci.yml` limpios (docker healthcheck ya usaba el endpoint DeltaOps). Sin env vars `SGMA_*`.
- `replit.md` actualizado (título DeltaOps + nota "SGMA retirado — nunca llegó a producción — reemplazado por DeltaOps" + referencias operativas corregidas). Docs históricas conservadas con banner.

## 9. Pruebas (FASE 11)

| Suite | Resultado |
|---|---|
| **api-server tests** | **220/220 PASS** (23 archivos; incluye identidad, RLS, RBAC, entitlements, E2E HTTP, todos los módulos) |
| typecheck (monorepo) | **PASS** |
| build (deltaops + api-server) | **PASS** |
| codegen / drift OpenAPI | **PASS** |
| prettier (gate CI: api-server/scripts/db `src`) | mis archivos editados limpios (`app.ts` reformateado); warnings restantes son **deuda preexistente** en archivos no tocados |
| lib tests (17 paquetes) | **16 PASS**; `module-ordenes` con fallos flaky (ver §11) |

Tras las suites (que truncan la DB dev) se re-ejecutó `pnpm --filter @workspace/api-server run seed:demo` (**seed DEMO completado**; users=120). El reinicio del workflow del API Server lo orquesta el agente principal.

## 10. Validación negativa + E2E (FASE 12)

Ejecutada contra una instancia del build (`node dist/index.mjs`, puerto aislado):
- **Rutas SGMA legacy → 404** (las 10: `/api/{assets,work-orders,spare-parts,locations,work-centers,technicians,suppliers,maintenance-plans,dashboard/summary,healthz}`).
- **DeltaOps health → 200**; anónimo `auth/me` → **401**.
- **Login**: SUPER_ADMIN, TENANT_ADMIN y los 4 roles empresariales del tenant demo → **200**. (Cuentas y contraseñas de desarrollo derivadas por variables de entorno conforme a la convención del programa; no se documentan valores.)
- **RBAC consola**: SUPER_ADMIN `admin/tenants` → **200**; TENANT_ADMIN `admin/tenants` → **403** (experiencia empresarial sin consola técnica).
- **Módulos con sesión real (delta-demo)**: `activos, ordenes, inventario, planes, referencia` → 200 con datos tenant-scoped; `costos/hechos, correctivo/solicitudes, manodeobra/catalogo, analytics/indicadores, abastecimiento/solicitudes` → 200.
- **Sesión**: logout → 204, `me` post-logout → 401.
- **Ningún error** de servidor referenciando tablas `public.*` eliminadas (grep del log = vacío).

**Validación en runtime dev (:80, post-reinicio de workflows):** rutas legacy → 404; `platform/health` → 200; login → 200 (verificado por el agente principal).

**E2E navegador real (post-retiro):**
- SUPER_ADMIN → consola SaaS en `/deltaops/administracion/saas` con tabla de tenants (DELTA-DEMO, DELTAOPS) — **PASS**; logout correcto.
- TENANT_ADMIN → dashboard de empresa; Activos (10), Órdenes, Inventario (13 ítems), Costos y Analytics cargan con datos; intento de `/administracion/saas` → redirigido sin consola; tema oscuro aplicado desde el diálogo «Apariencia» — **PASS**.
- TECNICO → bandeja «Mis órdenes — Asignadas a mí» carga (estado vacío legítimo) — **PASS**.
- Refresh (F5) en página interna mantiene sesión — **PASS**.
- **Análisis de anomalías reportadas por el agente E2E:** el log del api-server durante la ventana E2E registra **exclusivamente** llamadas `/api/deltaops/*` desde el frontend — los 404 de `/api/work-orders`/`/api/assets` visibles en el log provienen de los `curl` de verificación negativa del propio cierre (constatación de que las rutas ya no existen), no del frontend DeltaOps (`rg` de rutas/hooks legacy en `artifacts/deltaops/src` = 0). El `GET /` → 404 es un probe benigno fuera del base path. Los dos `400` en `inventario/catalogos/tipos|estados` (`KRN-VAL-001`) son **preexistentes** (diff vs `pre-retiro-sgma` en inventario/deltaops = vacío) y la página de Inventario carga con datos; queda anotado como deuda funcional menor ajena a esta fase.

## 11. Deuda residual / desviaciones

1. **`module-ordenes` PG tests flaky (NO regresión).** 1–3 fallos no deterministas por corrida (`module.pg.test.ts`, `sesion.pg.test.ts`) por contención PG entre suites que comparten la DB dev. Evidencia de no-relación con SGMA: (a) `git diff pre-retiro-sgma -- lib/module-ordenes/` = **vacío** (código byte-idéntico); (b) el módulo usa solo `deltaops.*`, **cero** referencias SGMA; (c) fallos distintos en cada corrida ("Conflicto de versión", "No encontrado: orden-trabajo"); (d) historial git documenta estabilizaciones previas ("testTimeout 30s justificado para integración PG bajo contención"). El equivalente E2E de órdenes pasó dentro de api-server (220/220).
2. **Prettier: deuda preexistente** en ~80 archivos `lib/db/src/schema/deltaops-*.ts` no modificados (ya no conformes antes de esta fase). No introducida por DGP-023.2; fuera de alcance.
3. **Reinicio del workflow API Server**: fuera del alcance del subagente; el runtime dev quedó abajo tras las suites. La corrección de código garantiza 404/200 (validado contra el build). El agente principal debe reiniciar el workflow.
4. **Nota `info.description`** de `openapi.yaml` y comentarios de retiro contienen la cadena "SGMA" (marcadores intencionales de trazabilidad DGP-023.2), no código ejecutable.

## 12. Revisión independiente (FASE 14 · R1)

- Ningún código SGMA ejecutable: **PASS** (grep global; restos = docs/backup/comentarios/nombre de migración).
- Ningún endpoint SGMA: **PASS** (404 en las 10 rutas).
- Tablas SGMA eliminadas: **PASS** (public=0).
- OpenAPI limpio + clientes correctos: **PASS**.
- Health gate funcionando: **PASS** (200 en `/api/deltaops/platform/health`).
- DeltaOps sin regresiones: **PASS** (220/220 api-server; typecheck/build; RBAC/módulos/sesión E2E). Excepción flaky documentada y descartada como regresión.
- mockup-sandbox intacto: **PASS**. Documentación histórica conservada: **PASS**. Backup realizado: **PASS**. Migración correcta (deltaops intacto): **PASS**. Git limpio (solo cambios del retiro + este doc + `.txt` de la directiva; `backups/` ignorado): **PASS**.
- Dependencias inesperadas que obliguen a DETENERSE: **NINGUNA**.

**R1: PASS.**

---

**DGP-023.2 — RETIRO DEFINITIVO DE SGMA: COMPLETADO (PASS).** SGMA desapareció (frontend, API, tablas, seed, contratos, config); DeltaOps continúa funcionando sin regresiones. Backup realizado antes del DROP; health gate migrado; OpenAPI/clientes limpios y regenerados; migración `0048` aplicada dejando `public` vacío y `deltaops.*` intacto. **Sin commit** (pendiente de Dirección). **DETENIDO tras el cierre; NO se inicia DGP-023.3.**
